machine:
  node:
    version: 6

test:
  pre:
    - google-chrome --version

  override:
    - $(npm bin)/eslint --debug --output-file ${CIRCLE_TEST_REPORTS}/lint.xml --format junit
    - REPORT_DIR=${CIRCLE_TEST_REPORTS} LOG_DIR=${CIRCLE_ARTIFACTS} node test.js
